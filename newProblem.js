//create a function that takes 2 integers of a string as an input and outputs the sum(also as a string)
let num1 = 1;
let num2 = 2;
//create function
function takeTwoIntegers(num1) {
  //change the string of the 2 numbers from string to numbers
  return num1.parseInt();
}
//return the output as a string
console.log(num1 + num2);
